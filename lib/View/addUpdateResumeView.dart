import 'package:flutter/material.dart';

class AddUpdateResumeView extends StatelessWidget {
  const AddUpdateResumeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: AppBar(
          title: Text("Add Resume"),
        ),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
